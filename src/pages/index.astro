---
/**
 * Landing: A spatial system. Not pages, not sections.
 * Black → points → constellation → center text.
 * No nav, no buttons. Only "Explore the mind".
 * When user scrolls — camera moves forward into the network.
 * Content is accessed via node clicks (overlay), not scroll.
 */
import PageLayout from '@layouts/PageLayout.astro';
import HeroManifesto from '@components/HeroManifesto.astro';
import NeuralCanvas from '@components/NeuralCanvas.astro';
---

<PageLayout
  title="Akshay Bajpai | AI Architect & Technology Leader — LLMs, RAG, ML Systems"
  description="Akshay Bajpai — AI Architect & Technology Leader. Expert in LLMs, RAG pipelines, scalable AI systems. Published researcher, 97.5% Master's distinction. Ex-CTO. Building AI products that ship."
  mainClass="page-main--hero"
  hideHeader
  hideFooter
>
  <NeuralCanvas />
  <div class="hero-viewport">
    <HeroManifesto />
  </div>
  <div id="explore" class="scroll-spacer" aria-hidden="true"></div>
</PageLayout>

<style>
  .hero-viewport {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    z-index: 2;
    pointer-events: none;
  }
  .hero-viewport .hero-cta {
    pointer-events: auto;
  }
  /* Transparent spacer: scroll drives camera into network. No content. */
  .scroll-spacer {
    height: 150vh;
    pointer-events: none;
  }
</style>
