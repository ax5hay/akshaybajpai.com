---
/**
 * Hero as placard: shown in overlay when user clicks "Back to intro" from node content.
 * No nav, no footer. Just the intro text in museum placard style.
 */
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout
  title="Akshay Bajpai | AI Architect & Technology Leader"
  description="Akshay Bajpai — AI Architect & Technology Leader. The Architecture of Intelligence."
  noIndex
>
  <main class="hero-placard" aria-label="Introduction">
    <div class="hero-placard-inner">
      <h1 class="hero-placard-title">
        <span class="hero-placard-name">AKSHAY BAJPAI</span>
        <span class="hero-placard-role">Architect of Systems.</span>
        <span class="hero-placard-role">Builder of Intelligence.</span>
      </h1>
      <p class="hero-placard-cta">Explore the mind</p>
    </div>
  </main>
</BaseLayout>

<style>
  .hero-placard {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-16);
    background: var(--color-bg);
  }
  .hero-placard-inner {
    max-width: 42rem;
    text-align: center;
  }
  .hero-placard-title {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin: 0 0 var(--space-8);
  }
  .hero-placard-name {
    font-family: var(--font-serif);
    font-size: clamp(2.25rem, 7vw, 4rem);
    font-weight: 400;
    letter-spacing: 0.02em;
    color: var(--color-text);
    line-height: 1.1;
  }
  .hero-placard-role {
    font-family: var(--font-sans);
    font-size: clamp(0.8rem, 2.2vw, 0.95rem);
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin-top: var(--space-3);
  }
  .hero-placard-cta {
    font-size: var(--text-sm);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin: 0;
  }
</style>
